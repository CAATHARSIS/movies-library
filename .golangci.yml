version: "2"

run:
  timeout: 5m
  modules-download-mode: readonly

linters:
  enable:
    - govet
    - staticcheck
    - gocritic
    - unused
    - ineffassign
    - bodyclose
    - goconst
  disable:
    - errcheck

formaters:
  enable:
    - gofmt
    - goimports